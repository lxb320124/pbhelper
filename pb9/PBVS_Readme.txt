PowerBuilder Version Selector,自动根据 .pbw 判断使用已安装的哪个版本PB来打开。方便使用多PB版本混合时使用。

使用方法：
将 pbvs.exe 解压到指定文件夹下，然后运行 pbvs.exe ，会提示做 .pbw 关联。确定做文件类型关联后，就可以在任何PB项目文件.pbw 上双击自动选择正确的PB版本打开了.
将 pbvs.exe 锁定到任务栏 ，操作多版本 PB ,意想不到的方便!